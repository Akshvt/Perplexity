{
  "version": 2,
  "installCommand": "cd Backend && npm install --legacy-peer-deps && cd ../Frontend && npm install",
  "buildCommand": "cd Frontend && NODE_PATH=$PWD/node_modules npm run build && cd .. && node scripts/copy-dist.mjs",
  "outputDirectory": "Backend/public",
  "functions": {
    "api/index.js": {
      "maxDuration": 60,
      "includeFiles": "Backend/src/**"
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
